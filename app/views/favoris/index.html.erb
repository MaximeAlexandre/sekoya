<div>
    <%@favoris.each do |booking| %>
        <div>
            <p> <%= booking.helper.first_name %> <%= booking.helper.last_name %></p>
            <%= render :partial => "remove_favoris", locals: { booking: booking } %>
        </div>
    <% end  %>
    <p>------------------</p>
    <%= link_to "Back", senior_path, class: "btn seko-senior-btn" %>
</div>
